A "Simple" Student Database
Assignment1
Name: Ian Stevens
Student Number: STVIAN004

The programs we where given and didnt change:
tokenlib, cmdline_parser.cpp, cmdline_parser.h and counter.h

How to use: when the usere makes the makefile the class driver.cpp will run. A menu will be displayed and will allowe the user to enter 9 options. 1-8 preforms file handeling methods and it will continu to ask you for a number until q is pressed to exit the menu. The read Database option will read from a txt file entered by the user or the default one is "students.txt". The format for a database text file is a "," seperats all the information even when changing students.

 
